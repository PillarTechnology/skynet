apply plugin: 'application'

mainClassName = 'com.pillar.skynet.facekiosk.application.Main'

dependencies {
    compile "ch.qos.logback:logback-classic:${project.logbackVersion}"
    compile "org.openpnp:opencv:${project.openCVVersion}"
}